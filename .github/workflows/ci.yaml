name: Docker Compose Actions Workflow
on: 
  push:
    branches: [main]
jobs:
  build:
    runs-on: Linux
    steps:
      - uses: actions/checkout@v2 
      - name: 'remove old files'
        run: sudo rm -rf /var/www/html/CV
      - name: 'move html files'
        run: sudo mv ~/actions-runner/_work/CV/CV /var/www/html
