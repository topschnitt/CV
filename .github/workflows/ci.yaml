name: Docker Compose Actions Workflow
on: 
  push:
    branches: [main]
jobs:
  build:
    runs-on: Linux
    steps:
      - uses: actions/checkout@v3
      - name: ls
        run: ls
      - name: Build the Docker Containers
        run: cd CV/CV && sudo docker compose up -d --build
